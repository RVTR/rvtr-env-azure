apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rvtr.fullname" . }}-test-connection"
  labels:
    {{- include "rvtr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rvtr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
